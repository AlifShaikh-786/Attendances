import bcrypt from "bcrypt";

const plainPassword = "Admin@123"; // change to your desired admin password
const saltRounds = 10;

bcrypt
  .hash(plainPassword, saltRounds)
  .then((hash) => {
    console.log("Hashed password:", hash);
  })
  .catch((err) => console.error(err));
